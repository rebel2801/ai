import React from 'react';
import ReactDOM from 'react-dom';
 // Import your global CSS styles if you have them
import App from './App'; // Import your main application component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
